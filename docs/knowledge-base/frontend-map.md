# Frontend Map

No frontend application detected in this codebase. The platform is backend-only (API services).

## Client Access Points

| Entry Point | Protocol | Notes |
|-------------|----------|-------|
| API Gateway | HTTP (port 8085) | Single entry point for all API calls |
| Keycloak | HTTP (port 8080) | Auth UI / token endpoint |
| Spring Boot Admin | HTTP (port 8084) | Monitoring dashboard (built-in UI) |
| Eureka Dashboard | HTTP (port 8761) | Service registry UI (built-in) |
